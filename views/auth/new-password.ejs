<!-- HEADER LINKS INCLUDE -->
<%- include('../includes/header.ejs') %>   

<!-- NAVBAR -->
<%- include('../includes/nav.ejs'); %>

	<!-- ERROR -->
	<!-- <% if(errorMsg){ %>
	<div class="container">
		<div class="alert alert-danger mx-auto d-block" style="width : 50%"><b><%= errorMsg %></b></div>
	</div>
	<% } %> -->

	<div class="container">
		<h1 class="text-center">Update New Password</h1>
    <br>
		<div class="row">
			<div class="col-sm-3"></div>
				<div class="col-sm-6">
					<div class="card shadow mb-5">
						<div class="card-body">
							<form action="/newPassword" method="POST">
								<div class="form-group">
									<label><b>New Password</b></label>
									<input type="password" class="form-control" name="password">
								</div>
                                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                                <input type="hidden" name="userId" value="<%= id %>">
                                <input type="hidden" name="token" value="<%= token %>">
								<button class="btn btn-primary btn-block mybtn-corner" type="submit">Update Password</button>
							</form>
						</div>
					</div>
				</div>
			<div class="col-sm-3"></div>
		</div>
	</div>

    
<br><br><br>

<!-- FOOTER -->
<%- include('../includes/footer.ejs') %>