<!-- HEADER LINKS INCLUDE -->
<%- include('../includes/header.ejs') %>   

<!-- NAVBAR -->
<%- include('../includes/nav.ejs'); %>
    
    <div class="container">
     <div class="row">
         <div class="col-sm-1"></div>
         <div class="col-sm-5">
             <div class="card shadow bg-white w-3">
                 <div class="card-body">
                   <img src="<%= product.image %>" class="card-img-top mx-auto d-block" style="max-height: 200px; max-width: 60%;">
                   <h4 class="text-center"><%= product.title %></h4>
                   <br>
                   <h5>Price : ₹ <%= product.price %></h5>
                   <p><small><%= product.description %></small></p>
                   <br>
                   <form action="/cart" method="POST" class="myForm">
                    <input type="hidden" name="productId" value="<%= product._id %>">
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                    <button class="btn btn-outline-primary btn-block mybtn-corner" type="submit">Add to Cart</button>
                  </form>
                 </div>
             </div>
         </div>
         <div class="col-sm-2"><br></div>
         <div class="col-sm-4">
             <br>
             <h3 class="text-center">Owner's Details</h3><hr><br><br>
            <h5>Name    : <span class="float-right"><%= username %></span></h5>
            <h5>Email   : <span class="float-right"><%= email %></span></h5>
            <br><br>
            <p><i>Feel Free to call the owner for more details and enquiry.</i></p>
         </div>
     </div>    
    </div>



<!-- FOOTER -->
<%- include('../includes/footer.ejs') %>